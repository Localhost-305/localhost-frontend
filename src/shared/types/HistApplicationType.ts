export interface HistApplicationType {
    year: number;                 // Ano da candidatura (ex: 2024)
    month: number;                // Mês da candidatura (ex: 1 para janeiro)
    quantityApplications: number; // Quantidade de candidaturas naquele mês (ex: 1000)
  }